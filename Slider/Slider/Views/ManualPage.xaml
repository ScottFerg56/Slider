<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:loc="clr-namespace:CamSlider.CustomControls"
             x:Class="CamSlider.Views.ManualPage"
			 Title="Manual">
    <ContentPage.Content>
		<Grid>
			<Grid.RowDefinitions>
				<RowDefinition Height="Auto" />
				<RowDefinition Height="Auto" />
				<RowDefinition Height="Auto" />
				<RowDefinition Height="Auto" />
			</Grid.RowDefinitions>
			<Label Grid.Row="0" Grid.Column="1" x:Name="LabelSlide" Text="{Binding SlidePosition}" XAlign="Center" FontSize="Large"/>
			<loc:FMSlider Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3" IsEnabled="{Binding Enabled}" Value="{Binding SlideVector, Mode=OneWayToSource}" x:Name="SliderSlide" Minimum="-100" Maximum="100"/>
			<Label x:Name="LabelPan" Grid.Row="2" Grid.Column="1" Text="{Binding PanPosition}" XAlign="Center" FontSize="Large"/>
			<Button x:Name="ButtonZero" Grid.Row="2" Grid.Column="2" IsEnabled="{Binding Enabled}" Command="{Binding PanZeroCommand}" Text="Zero" />
			<loc:FMSlider Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="3" IsEnabled="{Binding Enabled}" Value="{Binding PanVector, Mode=OneWayToSource}" x:Name="SliderPan" Minimum="-100" Maximum="100"/>
		</Grid>
	</ContentPage.Content>
</ContentPage>